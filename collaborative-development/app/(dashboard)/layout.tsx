/**
 * (dashboard) route group layout.
 * Minimal wrapper — actual role-specific layouts are inside each role folder.
 */
export default function DashboardGroupLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
